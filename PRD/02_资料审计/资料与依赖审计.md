# 资料与依赖审计

> 归属：02_资料审计

## 1. 第三方运行时依赖

| 包 | 用途 | 许可证 |
|---|---|---|
| `httpx` | OpenAI 兼容 Provider 的 HTTP 调用 | BSD-3-Clause |
| `rich` | CLI 终端美化（可选，缺失时降级为纯文本） | MIT |

开发依赖：`pytest`、`pytest-cov`、`ruff`、`mypy`。可选 `server` extra：`fastapi`、`uvicorn`。

## 2. 参考项目审计（deepwolf）

- 项目：https://github.com/JuneQQQ/deepwolf（MIT License）
- 关系：**仅作灵感来源**，用于理解产品能力与玩法；**未复用其源码、测试、Prompt 或目录结构**。
- 记录：仓库根目录 `THIRD_PARTY_NOTICES.md` 已保留 deepwolf 的 MIT 声明备查。

## 3. 密钥与合规

- API Key 只能从环境变量 `AIWEREWOLF_API_KEY`（及 `AIWEREWOLF_PROVIDER/MODEL/BASE_URL`）读取。
- `.env` 已 git-ignore，绝不提交密钥；`.env.example` 仅含占位符。

## 4. 资料清单

| 资料 | 位置 |
|---|---|
| 许可证 | `LICENSE`（MIT） |
| 第三方声明 | `THIRD_PARTY_NOTICES.md` |
| 变更记录 | `CHANGELOG.md` |
| 项目说明 | `README.md` |
| PRD 文档 | `PRD/` |

## 5. 待审计项（后续补充）

- 真实模型输出样本审计（需真实 LLM 对局后补充）。
- 各 Provider 预设（openai/deepseek/mimo/groq/openrouter）的接口一致性验证。
