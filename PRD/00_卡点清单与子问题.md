# 卡点清单与子问题

> 记录当前里程碑（单真人 × 6 AI 垂直闭环）的已知卡点与规则定稿记录。
> 更新于：2026-09-06

## 一、当前卡点

### K1 · 真实模型对局 ✅ 技术验收通过（关闭）
- 已固化：Provider=DeepSeek，Base URL=`https://api.deepseek.com`，Model=`deepseek-v4-flash`，非思考模式（`thinking={"type":"disabled"}`），JSON Object 输出，temperature=0.4，max_tokens=1200，Key 只从本地 `.env` 读取（未在任何输出/日志/回放/测试/Git 中记录）。
- 5 局固定种子（11/23/5/42/7）脚本人类座位 + 6 AI 真实模型：全部正常结束，0 网络失败中断；累计 120 次决策、84 个公开节点；总调用 120；每局延迟范围 P50≈2.35–2.59s、P95≈2.87–3.69s；5 局 `estimated_cost` 合计 0.2078 元（内部估算，非实际扣费）。
- 聚合：重试率 0.0%（<10% ✓），兜底率 0.0%（<5% ✓），首次失败 0 次。
- 关键修复（依据诊断）：模型在 evidence 输出裸 `E29` 引用（非合法 JSON）；此前全局 E 替换会篡改 reasoning 等字符串，已改为仅对 `evidence`/`fabricated_event` 字段值做兼容（有回归测试证明字符串字段原样保留）。
- 定向欺骗场景：真实模型输出满足规则的故意欺骗。
- 结论：K1 技术验收通过。真正真人 UX 体验验收留到可交互页面完成后进行。

### K2 · 狼人“怀疑变化”语义 ✅ 已解决
- 现状：新增 `threat_delta` / `threat_key_player` 字段，记录战略威胁相对上次的变化；狼人的怀疑演化现在可在回放中展示。

### K3 · “编造事实但分差 < 0.20”的自动判定 ✅ 已解决（有限）
- 现状：`fabricated_event` 必须引用**可见、携带事实（data 非空）且目标一致**的事件；文本填空不再能绕过。
- 残留：语义级“声称与事件内容相反”的自动核查仍未做（需 LLM/人工复核）。

### K4 · 非 room/session 路径不收集轨迹 ✅ 已解决
- 现状：抽取 `make_traced_decider`，`simulate` 现在也构建持久玩家并 append-only 收集轨迹，`--transcript` 输出含 `traces`。
- `arena` 保持聚合评测（其目标是统计，不逐条输出轨迹）。

## 二、后续里程碑（本轮明确不做）

- 单真人 WebSocket（已设计并实现：`ai_werewolf/server/ws.py` + `ai_werewolf/transport/queue.py` + `tests/test_ws.py`）
- 多真人混房
- ASR 语音
- Admin 后台 HTTP 化
- SQLite 战绩持久化
- 前端页面与部署

## 二-b、游戏平衡风险（非 K1 阻塞项）

- 离线基线（210 局）：狼人胜率 llm **89.5%** [84.6%, 93.0%] / random **73.8%** [67.5%, 79.3%]，狼方结构性偏强。
- **规则定稿（#5/#6 维持现状）不解决此偏强**；当前证据只支持「保留规则、继续观测」。
- 若未来要解决平衡，应**单独改角色/规则结构并重跑基线**，不能靠调欺骗阈值或人格数值代替。
- 脚本人类座位不能用于证明真人胜率、人格质量或可玩性；需独立真人体验测试。

## 三、规则定稿记录（2026-09-06，6 项全部定稿）

### 游戏规则（#5、#6）

5. **女巫用药**：每晚最多用**一种药**；**仅首夜可自救**（次夜起不能自救）；不能毒自己或死者；解药/毒药各全场限一次。→ 与当前实现一致，已定稿。
6. **平票重投**：首次平票**仅在并列最高票者中重投**；再次平票则**无人出局**（仅被放逐者有遗言）。→ 与当前实现一致，已定稿。

### 验收目标与参数（#1–#4）

1. **模型与 SLO**：真实模型验收基准固定 DeepSeek `deepseek-v4-flash`（非思考、JSON Object、temperature=0.4、max_tokens=1200）。下一阶段 SLO：**单次模型调用 P95 ≤ 4s**（不对整局设 P95，真人操作等待会使整局时长失去可比性）。成本：**单局估算 ≤ ¥0.06**（静态单价内部估算，需以供应商实际账单复核；当前 5 局约 ¥0.0416/局）。
2. **欺骗阈值**：维持 **0.20 不调参**。现有数据证明协议稳定，但缺「未标记超阈值 / 错误标记」统计；下一轮补这两项统计后再决定是否校准。
3. **六人格 6 维**：维持现状**不调参**。5 局只能证明技术稳定；先做真实玩家盲评或行为差异分析，避免为平衡问题误调人格。
4. **遗言**：维持当前限制（真人 30s / ≤200 字；AI ≤120 字，Prompt 建议 60–100 字）。无真实 UI 体验数据前不放宽。
